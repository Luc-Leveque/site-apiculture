{% extends 'documentation/show.html.twig' %}

{% block title %}Produits de la ruche{% endblock %}

{% block body %}

    <div class="col-sm">
        <div class="banner row justify-content-center">
            <h1 class="align-self-center">Produits de la ruche</h1>
        </div>
        <div class="row justify-content-center">
        <p></p>
        </div>
    </div>
    
    {% for article in articles %}
        {{ parent() }}
    {% endfor %}
    
    {% block return_button %}{% endblock %}
     <div class="row justify-content-start">
        <a class="btn btn-warning" href="{{ path('documentation_apiculture')}}">Retour</a>
    </div>
{% endblock %}