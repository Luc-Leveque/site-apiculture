{% extends 'base.html.twig' %}

{% block body %}
    <h1>Partie admnistration</h1>

    <ul>
    <li>Identifiant : {{app.user.id}}</li>
    <li>Mon pseudo : {{app.user.username}}</li>
    <li>Mon adresse mail : {{app.user.email}}</li>
    </ul>
    <a href="{{ path('user_edit')}}" class="btn btn-primary">Modifier ses informations</a>
    <a href="{{ path('user_change_password')}}" class="btn btn-primary">Modifier mon mot de passe</a>

{% endblock %}